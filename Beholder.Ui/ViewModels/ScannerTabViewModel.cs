using System;

namespace Beholder.Ui.ViewModels;

internal partial class ScannerTabViewModel : ViewModelBase, IDisposable {
    // Stub placeholder for a future Scanner tab. IDisposable scaffolded now
    // (audit #37) so MainWindowViewModel can dispose symmetrically once
    // subscriptions land.
    public void Dispose() { }
}
