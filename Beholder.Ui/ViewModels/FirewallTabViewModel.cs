using System;

namespace Beholder.Ui.ViewModels;

internal partial class FirewallTabViewModel : ViewModelBase, IDisposable {
    // Stub placeholder for Phase 6.4. Implements IDisposable now so
    // MainWindowViewModel.Dispose() can call it symmetrically once the
    // Firewall tab gains event subscriptions — audit #37 insurance.
    public void Dispose() { }
}
