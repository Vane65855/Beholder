using System;

namespace Beholder.Ui.ViewModels;

internal partial class SettingsTabViewModel : ViewModelBase, IDisposable {
    // Stub placeholder for Phase 5.5 (retention preset picker + recording
    // filter toggle, at minimum). IDisposable scaffolded now (audit #37) so
    // MainWindowViewModel can dispose symmetrically once subscriptions land.
    public void Dispose() { }
}
